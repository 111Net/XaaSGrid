# XaaSGrid Installation

Requirements:

- Docker
- Docker Compose
- Linux VM/VPS/Cloud Server

Installation:

git clone XaaSGrid

docker compose up -d

Validation:

curl http://localhost:4000/api/health

